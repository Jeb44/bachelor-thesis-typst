#import "../lib/lib.typ": abstract

#set align(center)
#set par(leading: 1.5em, justify: true)

#abstract([ Rust is a systems programming language that has gained significant popularity in recent years due to its focus on safety, performance, and concurrency, but the extendability is currently limited. In this thesis, we explore the advantages and disadvantages for a selection of Rust crates when designing a dynamic (does plugin system already imply it's dynamic?) plugin system.
])
